#include <bits/stdc++.h>
using namespace std;

struct TreeNode
{
    int data;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x)
    {
        data = x;
        left = NULL;
        right = NULL;
    }
};

// If we know the keys in advance, we can construct a balanced binary search tree in O(log n) time

int main()
{

    return 0;
}