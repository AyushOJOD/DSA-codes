#include <bits/stdc++.h>
using namespace std;

void operate(vector<int> &a)
{
    vector<int> cnt(n, 0);
}

void solve()
{
    int n;
    cin >> n;

    vector<int> a(n);
    for (int i = 0; i < n; i++)
        cin >> a[i];
}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}