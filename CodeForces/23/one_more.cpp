#include <bits/stdc++.h>
using namespace std;

int main()
{

    int t;
    cin >> t;
    while (t--)
    {
        int a, b, c, d;
        cin >> a >> b >> c >> d;

        int alice = 0, bob = 0;

        alice += a;
        bob += a;

        int minVal = min(b, c);
    }

    return 0;
}