#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n;
    cin >> n;

    vector<int> nums(n);

    for (int i = 0 i < n; i++)
    {
        cin >> nums[i];
    }

    int a = 0, b = 0, c = 0;

    for (int i = 0; i < n; i++)
    {
        if (nums[i] ==)
    }
}

int main()
{

    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}