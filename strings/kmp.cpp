// We need to find an proper prefix suffix array

#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s1, s2;
    cin >> s1;
    getline(cin, s2);
    cout << s1 << endl;
    cout << s2 << endl;
    cout << s2.size() << endl;

    return 0;
}