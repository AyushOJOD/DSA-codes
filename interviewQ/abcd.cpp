#include <bits/stdc++.h>
using namespace std;

vector<vector<string>> getStatForRequests(int m, vector<string> database, vector<string> requests)
{
    unordered_map<string, pair<int, string>> db;

    for (string &s : database)
    {
    }
}

int main()
{

    return 0;
}