#include<bits/stdc++.h>
using namespace std;

int firstOccour(int arr[])

int countOccurence(int arr[], int n, int k){
    int count=0;
    int low=0;
    int high=n-1;
    // We need to find first and last occourence and use [Last-First+1];
    int mid=(high+low)/2;

}

int main(){
    int n;
    cout<<"Enter the number of elements: ";
    cin>>n;

    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }

    return 0;
}