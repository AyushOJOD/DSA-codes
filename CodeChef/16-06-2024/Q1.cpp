#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        long long N;
        cin >> N;
        vector<int> V(N);
        for (int i = 0; i < n; i++)
        {
            cin >> pages[i];
        }
        sort(V.begin(), V.end());
        int mxPag = V[N - 1] + V[N - 2];
        cout << mxpag << endl;
    }

    return 0;
}