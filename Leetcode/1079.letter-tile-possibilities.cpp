#include <bits/stdc++.h>
using namespace std;

vector<string> solve(string tiles, vector<string> &res, int idx)
{
}

int numTilePossibilities(string tiles)
{
    vector<string> res;

    vector<string> ans = solve(tiles, res, idx);
}

int main()
{

    return 0;
}