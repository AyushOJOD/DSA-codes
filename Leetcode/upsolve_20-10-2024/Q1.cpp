#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    vector<string> stringSequence(string target)
    {
        vector<string> res;

        for (int i = 0; i < target.length(); i++)
        {
            char ch = target[i];

            int n = ch - 'a';

            for (int i = 0; i < n; i++)
            {
            }
        }
    }
};

int main()
{

    return 0;
}