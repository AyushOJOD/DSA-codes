#include <bits/stdc++.h>
using namespace std;

void bfs(int row, int col, vector<vector<int>> &land, vector<vector<int>> &vis, vector<int> &farmLand)
{
    int n = land.size();
    int m = land[0].size();

    vector<vector<int>> coord;
}

vector<vector<int>> findFarmland(vector<vector<int>> &land)
{
    int n = land.size();
    int m = land[0].size();

    queue<pair<int, int>> q;
    vector<vector<int>> vis(n, vector<int>(m, 0));
    vector<vector<int>> farmLand;

    while (!q.empty() {
        int row = q.front().first;
    })
}

int main()
{

    return 0;
}