#include <bits/stdc++.h>
using namespace std;

string breakPalindrome(string palindrome)
{
    if (palindrome.length() == 1)
        return "";

    for(auto it: palindrome){
        if(it != 'a' )
    }
}

int main()
{

    return 0;
}