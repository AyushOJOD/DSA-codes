#include <bits/stdc++.h>
using namespace std;

vector<int> findErrorNums(vector<int> &nums)
{
    int n = nums.size();
    vector<int> ans;

    sort(v.begin(), v.end());

    int dup = -1, missing = -1;

    

}

int main()
{

    return 0;
}