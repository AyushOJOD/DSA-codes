#include <bits/stdc++.h>
using namespace std;

class Solution
{
private:
    bool helper(vector<int> &nums, vector<int> &sum, int idx)
    {
    }

public:
    bool canPartitionKSubsets(vector<int> &nums, int k)
    {
        vector<int> sum(k, 0);
        int n = nums.size();
    }
};

int main()
{

    return 0;
}