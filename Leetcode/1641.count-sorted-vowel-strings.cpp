#include <bits/stdc++.h>
using namespace std;

class Solution
{
private:
    int helper(vector<char> &vowels, int currLen, int n)
    {
        // Base case
        if (currLen = n)
        {
        }
    }

public:
    int countVowelStrings(int n)
    {
        vector<char> vowels = {a, e, i, o, u};

        int currLen = 0;
    }
};

int main()
{

    return 0;
}