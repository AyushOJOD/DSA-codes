#include <bits/stdc++.h>
using namespace std;

int shortestPathBinaryMatrix(vector<vector<int>> &grid)
{
    vector<pair<int, int>> vis(grid.size(), vector<int>(grid[0].size(), 0));

    
}

int main()
{

    return 0;
}