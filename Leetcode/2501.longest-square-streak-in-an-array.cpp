#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int longestSquareStreak(vector<int> &nums)
    {
        unordered_set<int> set(begin(nums), end(nums));
        int n = nums.size();

        int cnt = 0;

        for (int x : nums)
        {
        }
    }
};

int main()
{

    return 0;
}