#include <bits/stdc++.h>
using namespace std;

class Solution
{
private:
    long long helper(vector<vector<int>> &ques, long long points, int idx)
    {
        if (idx >= ques.size() - 1)
        {
            return points;
        }
    }

public:
    long long mostPoints(vector<vector<int>> &questions)
    {
    }
};

int main()
{

    return 0;
}